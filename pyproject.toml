[project]
name = "mavrs-pi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gpiozero>=2.0.1",
    "pyyaml>=6.0.3",
]


[tool.uv]
system-site-packages = true
